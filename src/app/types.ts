export enum RoutePathEnum {
    HOME = '',
    ARTICLES = 'articles',
    LOGIN = 'login',
    NOT_FOUND = '**'
}

export enum RouteUrlEnum {
    HOME = '/',
    ARTICLES = '/articles',
    LOGIN = '/login'
}
